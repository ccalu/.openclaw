## Durable memory flush — 2026-03-30

- Lucca accidentally reset the session; we reconstructed the relevant thread by reading the 29 Mar review materials and repo docs instead of guessing.
- Important correction: previous memory had overemphasized Telegram/watchdog context. The real active thread on 30 Mar was implementation planning/execution for the Content Factory editing department, especially PM -> VO -> S1 -> S2 architecture and Claude Code coordination.
- PM (Production Manager) architecture was materially advanced today:
  - We created canonical docs:
    - `auto_content_factory/docs/contracts/pm_read_pub_contract.md`
    - `auto_content_factory/docs/contracts/pm_override_contract.md`
  - We aligned PM mirror/docs around these contracts.
  - We standardized the Google service-account credential path for OpenClaw/runtime use as:
    - `C:\secrets\content_factory\google_service_account.json`
  - Lucca confirmed the reused V3 service account has valid access to Sheets/Drive.
- Claude Code successfully implemented PM substrate v1 and then the override layer. Real outcomes:
  - PM can read the real PUB sheet, rank channels, support `auto_select`, `filtered_select`, `direct_select`, build canonical jobs, and validate them.
  - Real observed PUB state during testing:
    - 38 eligible channels
    - all with negative runway
    - `C001 PT` excluded because scripts are exhausted
    - `C004 FRA` was top urgency at runway `-17`
  - Durable principle confirmed: PM must treat PUB as operational truth, not strategic sovereignty; Tobias remains the upstream strategic layer.
- We intentionally formalized the PM operating modes as durable architecture:
  - `auto_select`
  - `filtered_select`
  - `direct_select`
  with authority order `direct_select > filtered_select > auto_select`.
  Critical rules:
  - `filtered_select` must not silently fall back to unrestricted auto mode.
  - `direct_select` must not silently choose another channel if the requested target is invalid.
- Claude Code successfully implemented VO minimal bootstrap on top of real PM jobs:
  - PM -> VO handoff works end-to-end.
  - VO creates runs under the canonical production root:
    - `C:\Users\User-OEM\Desktop\content-factory\auto_content_factory\production\{account}_{language}\{job_id}\`
  - Policy chosen: if the run directory already exists, fail explicitly with `run_already_exists` (safe, deterministic, no silent merging).
  - VO bootstrap writes real artefacts like `job.json`, `run_metadata.json`, `checkpoints/vo_init.json`.
- We investigated V3 script fetch/lock behavior before designing the next VO step. Durable findings from `content_factory_v3`:
  - Script selection rule is simple and important: pick the first row with `Status = "to do"`.
  - Lock is applied by immediately updating sheet status to `processing`.
  - Legacy sheet columns in V3 are roughly:
    - `E = Status`
    - `F = Processing_Started`
    - `G = Processing_Completed`
    - `H = Account_Used`
  - This V3 semantics should be reused for the new VO rather than inventing a new lock system early.
- Claude Code then implemented `script_fetch_and_lock` successfully with real integrations:
  - PM -> VO -> script fetch & lock works end-to-end with real data.
  - Example real result:
    - `direct_select 004/fr`
    - script row 6 selected: `Les Enfants de la Creuse`
    - real sheet lock applied (`Status -> processing`)
    - local script artefacts written under `production/.../inputs/script/`
    - run ended at `next_step = ready_for_s1`
- S1 (Pre-Production) work advanced substantially. Important architectural findings:
  - Canonical source for S1 is `docs/branches/01_PRE_PRODUCTION.md`; the workspace mirror is not reliable enough as source of truth.
  - Canonical naming for this sector should be `b1_`, not `s1_`.
  - S1 is already well-defined in docs as:
    - `scene_director`
    - `screenplay_validator`
    - `tts_polish`
    - `polish_validator`
    - `scene_bible_builder`
    - checkpoints by step
  - `scene_bible.json` is the core output and should live at the run root as a living document.
- Claude Code implemented S1 and passed tests, but there is an important durable nuance:
  - The deterministic/runtime pieces are solid and reusable:
    - validators
    - scene_bible builder
    - schema validator
    - checkpoint writer
    - supervisor base
  - But the LLM operator steps (`scene_director`, `tts_polish`) were implemented as direct Python OpenAI API calls (`gpt-4.1-mini`) instead of real OpenClaw operators using the planned runtime (GPT-5.4 mini OAuth / Qwen local).
  - Therefore Tobias’ judgment is:
    - treat S1 as a **functional prototype accepted for pipeline validation**
    - not as final production-runtime architecture
  - We should preserve this distinction explicitly and not let the temporary Python direct-call mode become accidental production standard.
- Lucca agreed with the Tobias recommendation to accept S1 as a prototype-functional slice and move on to S2 while keeping the runtime-architecture debt explicit.
- S2 investigation produced durable architecture conclusions:
  - Canonical doc: `docs/branches/02_AUDIO.md`, but review shows the sector is split between two versions.
  - The more mature conceptual direction is:
    - `tts_generator`
    - `audio_transcriber`
    - `transcription/ratio validator`
    - `audio_validator` (LLM only for borderline cases)
    - `timestamp_aligner`
    - `bible_updater`
  - This is superior to the older `timing_extractor`-only model.
- Durable V3 findings for S2 / audio:
  - Gemini TTS in V3 is already implemented as a serious reusable substrate (`shared/services/gemini_tts_service.py`).
  - The TTS request is driven by:
    - text
    - explicit `language_code`
    - explicit `voice_name`
  - Voice choice should come exclusively from the channel YAML config in the new S2; no extra heuristic speaker selection.
  - KMS already manages key acquisition/reporting/cooldown/availability centrally; the TTS client should not reinvent that logic.
  - Client-side Gemini TTS code still appropriately handles call-specific behavior like hard timeouts, empty responses, content-block inference, and PCM->WAV packing.
- Durable V3 findings for WhisperX / validation / alignment:
  - WhisperX is managed as a local service that can be started/stopped/health-checked; Lucca says it stays off by default to avoid consuming GPU.
  - V3 has a mature `TranscriptionValidator` with language-specific thresholds, ignore patterns, exclude words, WER, and match ratio logic.
  - Example thresholds in V3 are language-specific and should strongly influence the new S2 (not a single universal threshold):
    - `en 0.60`, `fr 0.65`, `de 0.65`, `es 0.70`, `it 0.70`, `pt 0.75`, `ja 0.75`, `ko 0.75`, `ar 0.80`, `ru 0.82`, `pl 0.82`, default `0.85`.
  - V3 also already contains useful positional alignment logic using `SequenceMatcher` to transfer WhisperX timestamps back onto original script words when WhisperX mishears tokens (e.g. `911 -> 1091`). This is strong evidence that the new S2 should explicitly include `timestamp_aligner`, not just raw timestamp extraction.
- Durable architecture recommendation for S2 going forward:
  - TTS should allow controlled parallelism with a supervisor-level `TTS_CONCURRENCY_CAP`.
  - WhisperX should remain serialized / carefully controlled (GPU/local-service bound).
  - The likely OpenClaw-era layering for S2 is:
    - Python/runtime skills for:
      - `tts_generator`
      - `audio_transcriber`
      - `transcription_validator`
      - `timestamp_aligner`
      - `bible_updater`
    - one OpenClaw LLM operator only where it adds semantic judgment:
      - `audio_validator`
    - an `SM-S2` supervisor coordinating retries, parallelism, and checkpoints.
